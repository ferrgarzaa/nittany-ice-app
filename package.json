{
  "name": "nittany-ice-app",
  "version": "1.0.0",
  "description": "Wildhorn Trail Running Association — a community of trail runners.",
  "type": "module",
  "scripts": {
    "start": "web-dev-server --node-resolve --watch --open",
    "dev": "web-dev-server --node-resolve --watch --open",
    "build": "rimraf dist && rollup -c",
    "serve": "web-dev-server --root-dir dist --node-resolve --open"
  },
  "dependencies": {
    "lit": "^3.1.0",
    "@haxtheweb/d-d-d": "^10.0.0"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^15.2.3",
    "@web/dev-server": "^0.4.2",
    "@web/rollup-plugin-copy": "^0.5.1",
    "esbuild": "^0.20.0",
    "rimraf": "^5.0.5",
    "rollup": "^4.9.0",
    "rollup-plugin-esbuild": "^6.1.0"
  }
}
